{
  "name": "millenium-bim-call-center",
  "version": "0.0.1",
  "description": "",
  "author": "Jorge Esteves <jorge.esteves@itsector.pt>",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/@config/*",
    "e2e"
  ],
  "scripts": {
    "analyze": "turbo run analyze",
    "rsdoctor": "turbo run rsdoctor",
    "build": "turbo run build",
    "build:local": "cross-env-shell \"HOST_BASE_URL=http://localhost:${REMOTES_PORT:-3333}/ turbo run build\"",
    "dev": "turbo run dev",
    "types:generate": "turbo run types:generate",
    "types:build": "turbo run types:build",
    "biome:check": "turbo run biome:check",
    "biome:check:apply": "turbo run biome:check:apply",
    "biome:staged": "turbo run biome:check:apply -- --staged",
    "biome:lint": "turbo run biome:lint",
    "biome:format": "turbo run biome:format",
    "test": "turbo run test --summarize --continue=always",
    "test:ci": "turbo run test:ci",
    "test:watch": "turbo run test:watch",
    "e2e": "turbo run e2e --filter=e2e",
    "e2e:headed": "turbo run e2e:headed --filter=e2e",
    "e2e:show-report": "turbo run e2e:show-report --filter=e2e",
    "typecheck": "turbo run typecheck",
    "ci": "bun run types:build && bun run typecheck && bun run test:ci && bun run biome:check",
    "clean": "rimraf apps/*/node_modules packages/*/node_modules bun.lock node_modules",
    "prepare": "husky",
    "preview": "bunx cross-env-shell \"SHELL_PREVIEW_PORT=8080 SHARED_PREVIEW_PORT=8081 HEADER_PAGES_PREVIEW_PORT=8082 SIDEBAR_PAGES_PREVIEW_PORT=8083 RECORDS_PAGES_PREVIEW_PORT=8084 DOCUMENTATION_PAGES_PREVIEW_PORT=8085 SHARED_HOST_BASE_URL=http://localhost:8081/ HEADER_PAGES_HOST_BASE_URL=http://localhost:8082/ SIDEBAR_PAGES_HOST_BASE_URL=http://localhost:8083/ RECORDS_PAGES_HOST_BASE_URL=http://localhost:8084/ DOCUMENTATION_PAGES_HOST_BASE_URL=http://localhost:8085/ turbo run build --force && bun run preview:serve\"",
    "preview:serve": "concurrently --kill-others-on-fail -n shell,shared,header,sidebar,records,docs -c green,magenta,cyan,blue,yellow,red \"cd apps/shell && bunx serve -c serve.json -l 8080\" \"cd packages/shared && bunx serve -c serve.json -l 8081\" \"cd apps/header-pages && bunx serve -c serve.json -l 8082\" \"cd apps/sidebar-pages && bunx serve -c serve.json -l 8083\" \"cd apps/records-pages && bunx serve -c serve.json -l 8084\" \"cd apps/documentation-pages && bunx serve -c serve.json -l 8085\"",
    "preview:serve:win": "concurrently -n shell,shared,header,sidebar,records,docs -c green,magenta,cyan,blue,yellow,red \"cd apps/shell && bunx serve -s dist -l 8080\" \"cd packages/shared && bunx serve -s dist -l 8081\" \"cd apps/header-pages && bunx serve -s dist -l 8082\" \"cd apps/sidebar-pages && bunx serve -s dist -l 8083\" \"cd apps/records-pages && bunx serve -s dist -l 8084\" \"cd apps/documentation-pages && bunx serve -s dist -l 8085\""
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.6",
    "@commitlint/config-conventional": "^20.0.0",
    "commitlint": "^20.1.0",
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "dotenv-cli": "^10.0.0",
    "http-server": "^14.1.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.4",
    "rimraf": "^6.0.1",
    "serve": "^14.2.5",
    "turbo": "^2.5.8"
  },
  "engines": {
    "node": ">=18.0.0",
    "bun": ">=1.0.0"
  },
  "packageManager": "bun@1.3.0"
}
