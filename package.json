{
  "name": "millenium-bim-call-center",
  "version": "0.0.1",
  "description": "",
  "author": "Jorge Esteves <jorge.esteves@itsector.pt>",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/@config/*",
    "e2e"
  ],
  "scripts": {
    "analyze": "turbo run analyze",
    "build": "turbo run build",
    "build:local": "cross-env HOST_BASE_URL=\"http://localhost:3333/\" turbo run build",
    "serve:local:remotes": "serve . -l 3333 -s --cors",
    "serve:local:main": "serve apps/main/dist -l 8080 -s --cors",
    "serve:local:start": "bun run serve:local:remotes && bun run serve:local:main",
    "serve:local": "bun run build:local && bun run serve:local:start",
    "serve": "bun run serve:local",
    "dev": "turbo run dev",
    "types:generate": "turbo run types:generate",
    "types:build": "turbo run types:build",
    "biome:check": "turbo run biome:check",
    "biome:check:apply": "turbo run biome:check:apply",
    "biome:staged": "turbo run biome:check:apply -- --staged",
    "biome:lint": "turbo run biome:lint",
    "biome:format": "turbo run biome:format",
    "test": "turbo run test",
    "test:ci": "turbo run test:ci",
    "test:watch": "turbo run test:watch",
    "e2e": "turbo run e2e --filter=e2e",
    "e2e:headed": "turbo run e2e:headed --filter=e2e",
    "e2e:show-report": "turbo run e2e:show-report --filter=e2e",
    "typecheck": "turbo run typecheck",
    "ci": "bun run types:build && bun run typecheck && bun run test:ci && bun run biome:check",
    "clean": "rimraf apps/*/node_modules packages/*/node_modules bun.lock node_modules",
    "prepare": "husky",
    "preview": "bunx cross-env-shell \"SHELL_PREVIEW_PORT=8080 SHARED_PREVIEW_PORT=8081 HEADER_PAGES_PREVIEW_PORT=8082 SHARED_HOST_BASE_URL=http://localhost:8081/ HEADER_PAGES_HOST_BASE_URL=http://localhost:8082/ turbo run build --force && bun run preview:serve\"",
    "preview:serve": "concurrently --kill-others-on-fail -n shell,shared,header -c green,magenta,cyan \"cd apps/shell && bunx serve -s dist -l 8080\" \"cd packages/shared && bunx serve -s dist -l 8081\" \"cd apps/header-pages && bunx serve -s dist -l 8082\"",
    "preview:serve:win": "concurrently -n shell,shared,header -c green,magenta,cyan \"cd apps/shell && bunx serve -s dist -l 8080\" \"cd packages/shared && bunx serve -s dist -l 8081\" \"cd apps/header-pages && bunx serve -s dist -l 8082\""
  },
  "devDependencies": {
    "dotenv-cli": "^10.0.0",
    "@commitlint/config-conventional": "^19.8.1",
    "@biomejs/biome": "^2.2.4",
    "commitlint": "^19.8.1",
    "cross-env": "^10.0.0",
    "husky": "^9.1.7",
    "concurrently": "^9.2.1",
    "lint-staged": "^16.1.6",
    "rimraf": "^6.0.1",
    "serve": "^14.2.5",
    "turbo": "^2.5.6"
  },
  "engines": {
    "node": ">=18.0.0",
    "bun": ">=1.0.0"
  },
  "packageManager": "bun@1.2.20"
}
